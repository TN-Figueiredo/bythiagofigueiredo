/**
 * Contact form consent text version.
 *
 * Server-only source of truth — never trust the version string from form data.
 * Bump when the consent copy changes in `<ContactForm>` so the audit trail
 * pins each submission to the text the user actually saw.
 */
export const CONTACT_CONSENT_VERSION = 'contact-v1-2026-04'
export const CONTACT_MARKETING_CONSENT_VERSION = 'contact-marketing-v1-2026-04'

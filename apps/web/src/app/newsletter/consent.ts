/**
 * Newsletter consent text version.
 *
 * Server-only source of truth — never trust the version string from form data.
 * Bump when the consent copy in the signup form changes.
 */
export const NEWSLETTER_CONSENT_VERSION = 'newsletter-v1-2026-04'
